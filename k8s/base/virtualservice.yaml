apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: billing-api
  labels:
    app.kubernetes.io/name: billing-api
spec:
  hosts:
    - billing-api.dev.platform.ohanyere.internal
  gateways:
    - istio-system/platform-gateway
  http:
    - route:
        - destination:
            host: billing-api
            port:
              number: 80
          weight: 100
